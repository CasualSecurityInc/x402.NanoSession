# NanoSession x402 Integration Test Environment
# Copy this file to e2e.env and fill in your values
# DO NOT commit e2e.env to git (it's gitignored)

# Test wallet seed (64 character hex string)
# This wallet must have a small amount of XNO for testing
NANO_TEST_SEED=your_64_char_hex_seed_here

# Nano RPC endpoint URL
# Default: https://rpc.nano.to (community node)
NANO_RPC_URL=https://rpc.nano.to

# Server receiving address (nano_...)
# Payments will be sent to this address during tests
NANO_SERVER_ADDRESS=nano_your_receiving_address_here

# Maximum spend per test run (in raw)
# 0.001 XNO = 1000000000000000000000000000 raw
# Keep this small to avoid accidental large spends
NANO_MAX_SPEND=1000000000000000000000000000
